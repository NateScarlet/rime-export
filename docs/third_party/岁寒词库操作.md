> 岁寒输入法不支持导入其它输入法的原生词库，比如.bin 文件，但支持导入文本格式的词条文件，即.txt 文件，可以用记事本程序打开直接查看里面的内容；

## 支持的格式类型

目前岁寒输入法支持五种词条格式；

```
格式1：'pin'yin 拼音
格式2：拼音 'pin'yin
格式3：拼音 pin|yin 123
格式4：拼音(pin|yin)123
格式5：拼音 123 pin yin
```

只有在正确的格式下，岁寒输入法才能成功地导入其中的内容，因此请在导入之前查看一下文本的格式，并确保文件的编码格式为 UTF-8。
